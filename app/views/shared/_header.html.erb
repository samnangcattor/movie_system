<nav class="navbar navbar-default navbar-fixed-top">
  <div id="navbar" class="navbar-collapse collapse">
    <div class="container">
      <div class="row">
        <div class="col-sm-2 logo">
          logo movie
        </div>
        <div class="col-sm-7 dynamic_menu">
          dynamic menu
        </div>
        <div class="col-sm-3 header_right">
          <div class="col-sm-7">
            header right
          </div>
          <div class="col-sm-5">
            <% if current_user.nil? %>
              <%= link_to t("layouts.login"), "javascript:void(0)", class: "link_login",
                data: {toggle: "modal", target: "#modal_form_login"} %>
            <% else %>
            <li id="fat-menu" class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                <%= current_user.name %>
              <b class="caret"></b></a>
              <ul class="dropdown-menu">
                <li><%= link_to t("layouts.profile"), "#" %></li>
                <li>
                  <%= link_to t("layouts.setting"), "#" %>
                </li>
                <li>
                  <%= link_to t("layouts.logout"), destroy_user_session_path,
                  method: :delete %>
                </li>
              </ul>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
</nav>
<%= render "shared/form_login" %>
