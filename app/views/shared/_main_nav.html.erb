<div id="main-nav">
  <div id="myCarousel" class="carousel slide" data-ride="carousel">
    <% if @slideshows.present? %>
      <ol class="carousel-indicators">
        <li data-target= "#myCarousel" data-slide-to="0" class="active"></li>
        <% @slideshows.drop(1).each_with_index do |movie, index| %>
          <li data-target="#myCarousel" data-slide-to="<%= index + 1 %>"></li>
        <% end %>
      </ol>

      <div class="carousel-inner" role="listbox">
        <div class="item active">
          <%= link_to @slideshows.first do %>
            <%= image_tag @slideshows.first.link_cover, class: "resize-photo" %>
          <% end %>
        </div>
        <% @slideshows.drop(1).each do |movie| %>
          <div class="item">
            <%= link_to movie do %>
              <%= image_tag movie.link_cover, class: "resize-photo" %>
            <% end %>
          </div>
        <% end %>
      </div>

      <a class="left carousel-control" href="#myCarousel" role="button" data-slide="prev">
        <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
        <span class="sr-only">Previous</span>
      </a>
      <a class="right carousel-control" href="#myCarousel" role="button" data-slide="next">
        <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
        <span class="sr-only">Next</span>
      </a>
    <% end %>
  </div>
</div>
