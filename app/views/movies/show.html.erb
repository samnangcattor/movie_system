<% meta title: t("app_movie", movie: @movie.title), description: @movie.description,
  keywords: "movies, khmer, moviehd, moviehdkh, watch, hd, cartoon, drama, " + @movie.title,
  fb: {app_id: "161434647542133"},
  og: {
    title: @movie.title,
    description: @movie.description,
    url: "http://www.moviehdkh.com/movies/#{@movie.id}",
    image: @movie.photo,
    type: "website",
    site_name: "MoviehdKH"
  } %>
<% content_for :context_mid do %>
  <%= render "form" %>
<% end %>
